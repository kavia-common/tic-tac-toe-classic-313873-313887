# Tic Tac Toe Frontend (React)

Two-player Tic Tac Toe with win/draw detection and restart.

## Scripts

- `npm start` - Start dev server (port 3000 by default)
- `npm run build` - Production build
- `npm run lint` - Lint source
- `npm run format` - Format source
